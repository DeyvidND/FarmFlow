# AI Newsletter Auto-Draft Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A weekly Thursday-08:00-Sofia cron that, for each opted-in tenant, builds an AI-written "what's fresh" newsletter **draft** from the catalog and notifies the farmer — who reviews and sends it via the existing billed composer (the platform never auto-sends).

**Architecture:** A pure block assembler + an OpenAI copy writer (with deterministic fallback) + a `NewsletterDraftService` (gather products → write copy → assemble → `createCampaign` draft → notify) + a BullMQ weekly processor, all inside the existing `newsletter` module. One idempotent migration adds a `auto_generated` dedup flag. A backend opt-in toggle endpoint; the farmer checkbox UI is a deliberate follow-up.

**Tech Stack:** NestJS, BullMQ repeatable jobs, drizzle-orm, OpenAI SDK (already a dep), EmailService (global), Jest.

---

## File Structure

- Create `packages/db/drizzle/0073_newsletter_auto_generated.sql` — idempotent column add.
- Modify `packages/db/src/schema.ts` — add `autoGenerated` to `newsletterCampaigns`.
- Create `server/src/modules/newsletter/auto-draft.render.ts` — pure types + `assembleNewsletterBlocks`.
- Create `server/src/modules/newsletter/auto-draft.render.spec.ts`.
- Create `server/src/modules/newsletter/auto-draft.ai.ts` — `NewsletterCopyService` (OpenAI + fallback).
- Create `server/src/modules/newsletter/auto-draft.ai.spec.ts`.
- Create `server/src/modules/newsletter/newsletter-draft.service.ts` — gather + orchestrate + notify.
- Create `server/src/modules/newsletter/newsletter-draft.service.spec.ts`.
- Create `server/src/modules/newsletter/newsletter-draft.processor.ts` — weekly cron.
- Create `server/src/modules/newsletter/dto/auto-settings.dto.ts` — `{ enabled: boolean }`.
- Modify `server/src/common/queue/queue.constants.ts` — `NEWSLETTER_DRAFT_QUEUE`.
- Modify `server/src/modules/newsletter/newsletter.module.ts` — queue + providers.
- Modify `server/src/modules/newsletter/newsletter.controller.ts` — 3 endpoints (GET/PATCH auto-settings, POST auto-draft-test).

Test cmd: `pnpm --filter @fermeribg/api test <pattern>`. Build: `pnpm --filter @fermeribg/api build`. DB build: `pnpm --filter @fermeribg/db build`.

---

## Task 1: Migration + schema flag

**Files:**
- Create: `packages/db/drizzle/0073_newsletter_auto_generated.sql`
- Modify: `packages/db/src/schema.ts` (the `newsletterCampaigns` table, ~line 731)

- [ ] **Step 1: Write the migration**

Create `packages/db/drizzle/0073_newsletter_auto_generated.sql`:

```sql
ALTER TABLE newsletter_campaigns ADD COLUMN IF NOT EXISTS auto_generated boolean NOT NULL DEFAULT false;
```

- [ ] **Step 2: Register the migration in the journal**

Open `packages/db/drizzle/meta/_journal.json`, look at how `0072` is registered (the last `entries[]` item: `idx`, `version`, `when`, `tag`, `breakpoints`). Append an analogous entry for `0073` with `tag: "0073_newsletter_auto_generated"` and `idx` one higher than 0072's. Use the same `version` and `breakpoints` values 0072 uses; for `when`, copy 0072's value + 1000 (the exact epoch does not matter for a hand-written migration, only that the entry exists and ordering is preserved). This mirrors how prior hand-written migrations (the repo's `drizzle generate` is broken since 0059) were added.

- [ ] **Step 3: Add the column to the schema**

In `packages/db/src/schema.ts`, in the `newsletterCampaigns` table definition, add after the `priceStotinki` line:

```ts
  // Set true on drafts created by the weekly auto-draft cron (so it can dedup —
  // skip a tenant that still has an unreviewed auto-draft). Farmer/manual drafts
  // stay false.
  autoGenerated: boolean('auto_generated').notNull().default(false),
```

(`boolean` is already imported in this file.)

- [ ] **Step 4: Build the db package**

Run: `pnpm --filter @fermeribg/db build`
Expected: builds, no TS errors. (No unit test for a raw-SQL migration; the column is exercised by Task 4's service.)

- [ ] **Step 5: Commit**

```bash
git add packages/db/drizzle/0073_newsletter_auto_generated.sql packages/db/drizzle/meta/_journal.json packages/db/src/schema.ts
git commit -m "feat(db): newsletter_campaigns.auto_generated (migr 0073, idempotent)"
```

---

## Task 2: Pure block assembler

**Files:**
- Create: `server/src/modules/newsletter/auto-draft.render.ts`
- Test: `server/src/modules/newsletter/auto-draft.render.spec.ts`

- [ ] **Step 1: Write the failing test**

Create `server/src/modules/newsletter/auto-draft.render.spec.ts`:

```ts
import { assembleNewsletterBlocks, type DraftCopy, type DraftProduct } from './auto-draft.render';

const COPY: DraftCopy = { subject: 'Свежо тази седмица', intro: 'Здравейте!', blurbs: { Домати: 'Сладки и сочни' } };
const P = (over: Partial<DraftProduct> = {}): DraftProduct => ({ id: 'p1', name: 'Домати', priceStotinki: 250, imageUrl: null, ...over });

describe('assembleNewsletterBlocks', () => {
  it('starts with a heading from the subject and an intro text block', () => {
    const b = assembleNewsletterBlocks(COPY, [P()], 'https://shop.bg');
    expect(b[0]).toEqual({ type: 'heading', text: 'Свежо тази седмица', level: 1 });
    expect(b[1]).toEqual({ type: 'text', html: '<p>Здравейте!</p>' });
  });

  it('renders an image block (linked to the shop) when the product has an image', () => {
    const b = assembleNewsletterBlocks(COPY, [P({ imageUrl: 'https://cdn/x.jpg' })], 'https://shop.bg');
    expect(b).toContainEqual({ type: 'image', image: 'https://cdn/x.jpg', alt: 'Домати', href: 'https://shop.bg' });
  });

  it('omits the image block when there is no image, keeping the product text', () => {
    const b = assembleNewsletterBlocks(COPY, [P({ imageUrl: null })], 'https://shop.bg');
    expect(b.some((x) => x.type === 'image')).toBe(false);
    expect(b).toContainEqual({ type: 'text', html: '<p><b>Домати</b> — 2,50 €<br>Сладки и сочни</p>' });
  });

  it('shows just name + price when the blurb is missing', () => {
    const b = assembleNewsletterBlocks({ ...COPY, blurbs: {} }, [P()], 'https://shop.bg');
    expect(b).toContainEqual({ type: 'text', html: '<p><b>Домати</b> — 2,50 €</p>' });
  });

  it('puts a divider between products and a final shop button', () => {
    const b = assembleNewsletterBlocks(COPY, [P({ id: 'a', name: 'Домати' }), P({ id: 'b', name: 'Мед', priceStotinki: 1200 })], 'https://shop.bg');
    expect(b.some((x) => x.type === 'divider')).toBe(true);
    expect(b[b.length - 1]).toEqual({ type: 'button', label: 'Виж всички продукти', href: 'https://shop.bg' });
  });

  it('omits hrefs and the button when no shopUrl is given', () => {
    const b = assembleNewsletterBlocks(COPY, [P({ imageUrl: 'https://cdn/x.jpg' })]);
    expect(b).toContainEqual({ type: 'image', image: 'https://cdn/x.jpg', alt: 'Домати' });
    expect(b.some((x) => x.type === 'button')).toBe(false);
  });

  it('escapes HTML in names and blurbs', () => {
    const b = assembleNewsletterBlocks({ subject: 's', intro: '', blurbs: { 'A & B': '<x>' } }, [P({ name: 'A & B' })]);
    const txt = b.find((x) => x.type === 'text' && x.html.includes('A &amp; B'));
    expect(txt).toBeTruthy();
    expect(JSON.stringify(b)).not.toContain('<x>');
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm --filter @fermeribg/api test auto-draft.render`
Expected: FAIL — `Cannot find module './auto-draft.render'`.

- [ ] **Step 3: Write the renderer**

Create `server/src/modules/newsletter/auto-draft.render.ts`:

```ts
import type { NewsletterBlock } from '@fermeribg/types';

/** A product chosen to feature in an auto-draft. */
export interface DraftProduct {
  id: string;
  name: string;
  priceStotinki: number;
  imageUrl: string | null;
}

/** AI- (or fallback-) generated copy for an auto-draft. blurbs keyed by product name. */
export interface DraftCopy {
  subject: string;
  intro: string;
  blurbs: Record<string, string>;
}

function eur(stotinki: number): string {
  return (stotinki / 100).toFixed(2).replace('.', ',') + ' €';
}

function esc(s: string): string {
  return s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

/**
 * Build the newsletter body blocks from copy + featured products. Pure — the
 * caller passes the resolved shop URL (omitted → no links/button). The text
 * blocks are HTML-escaped here AND re-sanitized by createCampaign on save.
 */
export function assembleNewsletterBlocks(
  copy: DraftCopy,
  products: DraftProduct[],
  shopUrl?: string,
): NewsletterBlock[] {
  const blocks: NewsletterBlock[] = [];
  blocks.push({ type: 'heading', text: copy.subject, level: 1 });
  if (copy.intro.trim()) blocks.push({ type: 'text', html: `<p>${esc(copy.intro)}</p>` });

  products.forEach((p, i) => {
    if (i > 0) blocks.push({ type: 'divider' });
    if (p.imageUrl) {
      blocks.push({ type: 'image', image: p.imageUrl, alt: p.name, ...(shopUrl ? { href: shopUrl } : {}) });
    }
    const blurb = copy.blurbs[p.name]?.trim();
    blocks.push({
      type: 'text',
      html: `<p><b>${esc(p.name)}</b> — ${esc(eur(p.priceStotinki))}${blurb ? `<br>${esc(blurb)}` : ''}</p>`,
    });
  });

  if (shopUrl) blocks.push({ type: 'button', label: 'Виж всички продукти', href: shopUrl });
  return blocks;
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `pnpm --filter @fermeribg/api test auto-draft.render`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/newsletter/auto-draft.render.ts server/src/modules/newsletter/auto-draft.render.spec.ts
git commit -m "feat(newsletter): pure auto-draft block assembler"
```

---

## Task 3: AI copy writer

**Files:**
- Create: `server/src/modules/newsletter/auto-draft.ai.ts`
- Test: `server/src/modules/newsletter/auto-draft.ai.spec.ts`

- [ ] **Step 1: Write the failing test**

Create `server/src/modules/newsletter/auto-draft.ai.spec.ts`:

```ts
import { NewsletterCopyService } from './auto-draft.ai';

function makeSvc(key: string | null = 'sk-test') {
  const config = { get: (k: string, d?: unknown) => (k === 'OPENAI_API_KEY' ? key : d) } as any;
  return new NewsletterCopyService(config);
}
const PRODUCTS = [{ id: 'a', name: 'Домати', priceStotinki: 250, imageUrl: null }];

describe('NewsletterCopyService.writeCopy', () => {
  it('falls back deterministically when no API key is configured', async () => {
    const copy = await makeSvc(null).writeCopy('Зелена Ферма', PRODUCTS);
    expect(copy.subject).toContain('Зелена Ферма');
    expect(copy.blurbs).toEqual({});
  });

  it('parses a well-formed model response', async () => {
    const svc = makeSvc();
    (svc as any).client = { chat: { completions: { create: async () => ({ choices: [{ message: { content: JSON.stringify({ subject: 'Свежо!', intro: 'Здравейте', blurbs: { Домати: 'Сочни' } }) } }] }) } } };
    const copy = await svc.writeCopy('Ферма', PRODUCTS);
    expect(copy).toEqual({ subject: 'Свежо!', intro: 'Здравейте', blurbs: { Домати: 'Сочни' } });
  });

  it('falls back on malformed JSON', async () => {
    const svc = makeSvc();
    (svc as any).client = { chat: { completions: { create: async () => ({ choices: [{ message: { content: 'not json' } }] }) } } };
    const copy = await svc.writeCopy('Ферма Х', PRODUCTS);
    expect(copy.subject).toContain('Ферма Х');
    expect(copy.blurbs).toEqual({});
  });

  it('uses the fallback subject when the model omits one', async () => {
    const svc = makeSvc();
    (svc as any).client = { chat: { completions: { create: async () => ({ choices: [{ message: { content: JSON.stringify({ intro: 'само интро' }) } }] }) } } };
    const copy = await svc.writeCopy('Ферма Y', PRODUCTS);
    expect(copy.subject).toContain('Ферма Y');
    expect(copy.intro).toBe('само интро');
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm --filter @fermeribg/api test auto-draft.ai`
Expected: FAIL — module not found.

- [ ] **Step 3: Write the copy service**

Create `server/src/modules/newsletter/auto-draft.ai.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import OpenAI from 'openai';
import type { DraftProduct, DraftCopy } from './auto-draft.render';

const SYSTEM_PROMPT = `Ти си копирайтър за български фермерски магазин. Пишеш кратък седмичен бюлетин „какво е прясно".
Получаваш име на фермата и списък с продукти. Върни JSON:
{"subject": "...", "intro": "...", "blurbs": {"Име на продукт": "кратко описание до 12 думи"}}
- subject: кратко, примамливо заглавие на български (без емоджи спам).
- intro: 1–2 топли изречения, поздрав + покана да разгледат.
- blurbs: по едно кратко изречение за всеки продукт (ключът е ТОЧНО името на продукта).
Само JSON, без друг текст. Всичко на български.`;

@Injectable()
export class NewsletterCopyService {
  private readonly log = new Logger(NewsletterCopyService.name);
  private readonly client: OpenAI | null;
  private readonly model: string;

  constructor(config: ConfigService) {
    const key = config.get<string>('OPENAI_API_KEY');
    this.client = key ? new OpenAI({ apiKey: key, timeout: 30_000, maxRetries: 1 }) : null;
    this.model = config.get<string>('OPENAI_IMPORT_MODEL', 'gpt-4o-mini') ?? 'gpt-4o-mini';
  }

  /** Deterministic copy when AI is unavailable — a plain newsletter beats none. */
  private fallback(farmName: string): DraftCopy {
    return { subject: `Свежи продукти от ${farmName}`, intro: 'Вижте какво предлагаме тази седмица.', blurbs: {} };
  }

  /** Write subject + intro + per-product blurbs. Never throws — fallback on any failure. */
  async writeCopy(farmName: string, products: DraftProduct[]): Promise<DraftCopy> {
    if (!this.client) return this.fallback(farmName);
    try {
      const res = await this.client.chat.completions.create({
        model: this.model,
        response_format: { type: 'json_object' },
        messages: [
          { role: 'system', content: SYSTEM_PROMPT },
          { role: 'user', content: JSON.stringify({ farm: farmName, products: products.map((p) => p.name) }) },
        ],
      });
      const raw = res.choices[0]?.message?.content ?? '{}';
      const parsed = JSON.parse(raw) as { subject?: unknown; intro?: unknown; blurbs?: unknown };
      const subject = typeof parsed.subject === 'string' && parsed.subject.trim() ? parsed.subject.trim() : this.fallback(farmName).subject;
      const intro = typeof parsed.intro === 'string' ? parsed.intro.trim() : '';
      const blurbs: Record<string, string> = {};
      if (parsed.blurbs && typeof parsed.blurbs === 'object') {
        for (const [k, v] of Object.entries(parsed.blurbs as Record<string, unknown>)) {
          if (typeof v === 'string' && v.trim()) blurbs[k] = v.trim();
        }
      }
      return { subject, intro, blurbs };
    } catch (e) {
      this.log.warn(`newsletter copy AI failed, using fallback: ${String((e as Error)?.message ?? e)}`);
      return this.fallback(farmName);
    }
  }
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `pnpm --filter @fermeribg/api test auto-draft.ai`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/newsletter/auto-draft.ai.ts server/src/modules/newsletter/auto-draft.ai.spec.ts
git commit -m "feat(newsletter): AI copy writer for auto-draft (+ fallback)"
```

---

## Task 4: NewsletterDraftService

**Files:**
- Create: `server/src/modules/newsletter/newsletter-draft.service.ts`
- Test: `server/src/modules/newsletter/newsletter-draft.service.spec.ts`

- [ ] **Step 1: Write the failing test**

Create `server/src/modules/newsletter/newsletter-draft.service.spec.ts`:

```ts
import { NewsletterDraftService } from './newsletter-draft.service';

const PRODUCTS = [{ id: 'a', name: 'Домати', priceStotinki: 250, imageUrl: null }];

function makeSvc(over: {
  pending?: boolean;
  products?: any[];
  tenant?: { name: string; email: string | null; settings: unknown } | null;
} = {}) {
  const db = {} as any;
  const email = { sendMail: jest.fn().mockResolvedValue(undefined) };
  const config = { get: (k: string, d?: unknown) => (k === 'PUBLIC_APP_URL' ? 'https://panel.bg' : d) };
  const newsletter = { createCampaign: jest.fn().mockResolvedValue({ id: 'camp1' }) };
  const copy = { writeCopy: jest.fn().mockResolvedValue({ subject: 'Свежо', intro: 'Здр', blurbs: {} }) };
  const svc = new NewsletterDraftService(db, email as any, config as any, newsletter as any, copy as any);
  (svc as any).hasPendingAutoDraft = jest.fn().mockResolvedValue(over.pending ?? false);
  (svc as any).gatherFreshProducts = jest.fn().mockResolvedValue(over.products ?? PRODUCTS);
  (svc as any).loadTenant = jest.fn().mockResolvedValue(
    over.tenant ?? { name: 'Зелена Ферма', email: 'farm@bg', settings: { siteUrl: 'https://shop.bg' } },
  );
  (svc as any).markAutoGenerated = jest.fn().mockResolvedValue(undefined);
  return { svc, email, newsletter, copy };
}

describe('NewsletterDraftService.generateForTenant', () => {
  it('skips when an unreviewed auto-draft already exists', async () => {
    const { svc, newsletter } = makeSvc({ pending: true });
    expect(await svc.generateForTenant('t1')).toEqual({ created: false, reason: 'exists' });
    expect(newsletter.createCampaign).not.toHaveBeenCalled();
  });

  it('skips when there is no fresh content', async () => {
    const { svc, newsletter } = makeSvc({ products: [] });
    expect(await svc.generateForTenant('t1')).toEqual({ created: false, reason: 'no-content' });
    expect(newsletter.createCampaign).not.toHaveBeenCalled();
  });

  it('creates a draft, marks it auto-generated, and notifies the farmer', async () => {
    const { svc, newsletter, email, copy } = makeSvc();
    const res = await svc.generateForTenant('t1');
    expect(res).toEqual({ created: true, campaignId: 'camp1' });
    expect(copy.writeCopy).toHaveBeenCalledWith('Зелена Ферма', PRODUCTS);
    expect(newsletter.createCampaign).toHaveBeenCalledTimes(1);
    const arg = newsletter.createCampaign.mock.calls[0][1];
    expect(arg.subject).toBe('Свежо');
    expect(Array.isArray(arg.blocks)).toBe(true);
    expect((svc as any).markAutoGenerated).toHaveBeenCalledWith('camp1', 't1');
    expect(email.sendMail).toHaveBeenCalledTimes(1);
    expect(email.sendMail.mock.calls[0][0].to).toBe('farm@bg');
  });

  it('still creates the draft when the farm has no email (just no notify)', async () => {
    const { svc, email } = makeSvc({ tenant: { name: 'Ф', email: null, settings: {} } });
    const res = await svc.generateForTenant('t1');
    expect(res).toEqual({ created: true, campaignId: 'camp1' });
    expect(email.sendMail).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm --filter @fermeribg/api test newsletter-draft.service`
Expected: FAIL — module not found.

- [ ] **Step 3: Write the service**

Create `server/src/modules/newsletter/newsletter-draft.service.ts`:

```ts
import { Injectable, Inject, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { and, desc, eq, gt, gte, isNull, lte, sql } from 'drizzle-orm';
import {
  type Database,
  products,
  productAvailabilityWindows,
  orderItems,
  newsletterCampaigns,
  newsletterSubscribers,
  tenants,
} from '@fermeribg/db';
import { DB_TOKEN } from '../../common/drizzle/drizzle.constants';
import { EmailService } from '../../common/email/email.service';
import { bgToday } from '../../common/time/bg-time';
import { NewsletterService } from './newsletter.service';
import { NewsletterCopyService } from './auto-draft.ai';
import { assembleNewsletterBlocks, type DraftProduct } from './auto-draft.render';

const CAP = 6;
const NEW_DAYS = 7;

export type GenResult =
  | { created: true; campaignId: string }
  | { created: false; reason: 'exists' | 'no-content' };

@Injectable()
export class NewsletterDraftService {
  private readonly logger = new Logger(NewsletterDraftService.name);

  constructor(
    @Inject(DB_TOKEN) private readonly db: Database,
    private readonly email: EmailService,
    private readonly config: ConfigService,
    private readonly newsletter: NewsletterService,
    private readonly copy: NewsletterCopyService,
  ) {}

  /** Tenants opted in (settings.autoNewsletter=true), not demo, with ≥1 active subscriber. */
  async eligibleTenantIds(): Promise<string[]> {
    const rows = await this.db
      .select({ id: tenants.id })
      .from(tenants)
      .where(
        and(
          eq(tenants.isDemo, false),
          sql`coalesce(${tenants.settings}->>'autoNewsletter','false') = 'true'`,
          sql`exists (select 1 from ${newsletterSubscribers} ns where ns.tenant_id = ${tenants.id} and ns.unsubscribed_at is null)`,
        ),
      );
    return rows.map((r) => r.id);
  }

  /** Persist the opt-in flag (jsonb merge, mirroring tenants.service settings writers). */
  async setAutoNewsletter(tenantId: string, enabled: boolean): Promise<{ enabled: boolean }> {
    await this.db
      .update(tenants)
      .set({
        settings: sql`jsonb_set(coalesce(${tenants.settings}, '{}'::jsonb), array['autoNewsletter'], ${JSON.stringify(enabled)}::jsonb, true)`,
      })
      .where(eq(tenants.id, tenantId));
    return { enabled };
  }

  /** Current opt-in state for the tenant. */
  async getAutoNewsletter(tenantId: string): Promise<{ enabled: boolean }> {
    const [t] = await this.db
      .select({ v: sql<boolean>`coalesce(${tenants.settings}->>'autoNewsletter','false') = 'true'` })
      .from(tenants)
      .where(eq(tenants.id, tenantId))
      .limit(1);
    return { enabled: !!t?.v };
  }

  /** Does the tenant already have an unreviewed (draft) auto-generated campaign? */
  private async hasPendingAutoDraft(tenantId: string): Promise<boolean> {
    const [row] = await this.db
      .select({ id: newsletterCampaigns.id })
      .from(newsletterCampaigns)
      .where(
        and(
          eq(newsletterCampaigns.tenantId, tenantId),
          eq(newsletterCampaigns.status, 'draft'),
          eq(newsletterCampaigns.autoGenerated, true),
        ),
      )
      .limit(1);
    return !!row;
  }

  /** Mix: new (7d) + available-now + best-seller fallback, deduped, capped at CAP. */
  async gatherFreshProducts(tenantId: string): Promise<DraftProduct[]> {
    const cols = {
      id: products.id,
      name: products.name,
      priceStotinki: products.priceStotinki,
      imageUrl: products.imageUrl,
    };
    const live = and(eq(products.tenantId, tenantId), eq(products.isActive, true), isNull(products.deletedAt));

    const fresh = await this.db
      .select(cols)
      .from(products)
      .where(and(live, gte(products.createdAt, sql`now() - interval '${sql.raw(String(NEW_DAYS))} days'`)))
      .orderBy(desc(products.createdAt))
      .limit(CAP);

    const today = bgToday();
    const available = await this.db
      .selectDistinct(cols)
      .from(products)
      .innerJoin(productAvailabilityWindows, eq(productAvailabilityWindows.productId, products.id))
      .where(
        and(
          live,
          lte(productAvailabilityWindows.startsAt, today),
          gte(productAvailabilityWindows.endsAt, today),
          gt(productAvailabilityWindows.remaining, 0),
        ),
      )
      .limit(CAP);

    const merged: DraftProduct[] = [];
    const seen = new Set<string>();
    for (const p of [...fresh, ...available]) {
      if (seen.has(p.id)) continue;
      seen.add(p.id);
      merged.push({ id: p.id, name: p.name, priceStotinki: p.priceStotinki, imageUrl: p.imageUrl ?? null });
      if (merged.length >= CAP) break;
    }
    if (merged.length > 0) return merged;

    // Fallback: top sellers by order-item count (static-catalog farms still get a draft).
    const best = await this.db
      .select({ ...cols, n: sql<number>`count(${orderItems.id})::int` })
      .from(orderItems)
      .innerJoin(products, eq(orderItems.productId, products.id))
      .where(live)
      .groupBy(products.id, products.name, products.priceStotinki, products.imageUrl)
      .orderBy(desc(sql`count(${orderItems.id})`))
      .limit(CAP);
    return best.map((p) => ({ id: p.id, name: p.name, priceStotinki: p.priceStotinki, imageUrl: p.imageUrl ?? null }));
  }

  private async loadTenant(tenantId: string): Promise<{ name: string; email: string | null; settings: unknown } | null> {
    const [t] = await this.db
      .select({ name: tenants.name, email: tenants.email, settings: tenants.settings })
      .from(tenants)
      .where(eq(tenants.id, tenantId))
      .limit(1);
    return t ?? null;
  }

  private async markAutoGenerated(campaignId: string, tenantId: string): Promise<void> {
    await this.db
      .update(newsletterCampaigns)
      .set({ autoGenerated: true })
      .where(and(eq(newsletterCampaigns.id, campaignId), eq(newsletterCampaigns.tenantId, tenantId)));
  }

  private shopUrl(settings: unknown): string | undefined {
    const s = (settings ?? {}) as Record<string, unknown>;
    return typeof s.siteUrl === 'string' && s.siteUrl ? s.siteUrl : undefined;
  }

  private async notifyFarmer(email: string, campaignId: string): Promise<void> {
    const panel = this.config.get<string>('PUBLIC_APP_URL') ?? 'http://localhost:3000';
    const link = `${panel}/newsletter`;
    const html = `<!DOCTYPE html><html lang="bg"><body style="font-family:Arial,sans-serif;max-width:600px;margin:0 auto;padding:20px;color:#333">
  <h1 style="font-size:20px;color:#2d6a4f">Готов бюлетин за преглед</h1>
  <p style="font-size:14px;color:#555">Подготвихме чернова на седмичен бюлетин с пресните ти продукти. Прегледай, редактирай и изпрати с един клик.</p>
  <p><a href="${link}" style="display:inline-block;background:#2d6a4f;color:#fff;text-decoration:none;padding:10px 18px;border-radius:8px;font-weight:bold">Отвори бюлетина</a></p>
  <p style="font-size:12px;color:#999;margin-top:24px">ФермериБГ — автоматична чернова. Нищо не се изпраща без твоето одобрение.</p>
</body></html>`;
    const text = `Готов бюлетин за преглед\n\nПодготвихме чернова на седмичен бюлетин. Отвори: ${link}\n\nНищо не се изпраща без твоето одобрение.`;
    await this.email.sendMail({ to: email, subject: 'Готов бюлетин за преглед — ФермериБГ', html, text });
  }

  /** Build + save one tenant's weekly auto-draft. Dedup + skip-empty; never auto-sends. */
  async generateForTenant(tenantId: string): Promise<GenResult> {
    if (await this.hasPendingAutoDraft(tenantId)) return { created: false, reason: 'exists' };

    const featured = await this.gatherFreshProducts(tenantId);
    if (featured.length === 0) return { created: false, reason: 'no-content' };

    const tenant = await this.loadTenant(tenantId);
    const farmName = tenant?.name ?? 'Фермата';
    const copy = await this.copy.writeCopy(farmName, featured);
    const blocks = assembleNewsletterBlocks(copy, featured, this.shopUrl(tenant?.settings));

    const campaign = await this.newsletter.createCampaign(tenantId, { subject: copy.subject, blocks });
    await this.markAutoGenerated(campaign.id, tenantId);

    if (tenant?.email) {
      try {
        await this.notifyFarmer(tenant.email, campaign.id);
      } catch (e) {
        this.logger.warn(`[newsletter-draft] notify failed tenant=${tenantId}: ${String((e as Error)?.message ?? e)}`);
      }
    }
    this.logger.log(`[newsletter-draft] draft ${campaign.id} for tenant=${tenantId}`);
    return { created: true, campaignId: campaign.id };
  }
}
```

NOTE for the implementer: confirm the exact drizzle export names — `productAvailabilityWindows`, `orderItems`, `newsletterSubscribers`, `newsletterCampaigns` — in `packages/db/src/schema.ts` (they are defined there; the availability table is the `product_availability_windows` pgTable). If an export name differs, match it and report. The `createCampaign(tenantId, { subject, blocks })` signature is the existing `NewsletterService.createCampaign(tenantId, UpsertCampaignDto)` and returns `{ id, ... }`.

- [ ] **Step 4: Run the test to verify it passes**

Run: `pnpm --filter @fermeribg/api test newsletter-draft.service`
Expected: PASS (the 4 orchestration cases; db-touching methods are overridden in the test).

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/newsletter/newsletter-draft.service.ts server/src/modules/newsletter/newsletter-draft.service.spec.ts
git commit -m "feat(newsletter): auto-draft service (gather + generate + notify)"
```

---

## Task 5: Cron, queue, endpoints, module

**Files:**
- Modify: `server/src/common/queue/queue.constants.ts`
- Create: `server/src/modules/newsletter/newsletter-draft.processor.ts`
- Create: `server/src/modules/newsletter/dto/auto-settings.dto.ts`
- Modify: `server/src/modules/newsletter/newsletter.module.ts`
- Modify: `server/src/modules/newsletter/newsletter.controller.ts`

- [ ] **Step 1: Add the queue constant**

In `server/src/common/queue/queue.constants.ts`, add after the existing constants:

```ts
export const NEWSLETTER_DRAFT_QUEUE = 'newsletter-draft';
```

- [ ] **Step 2: Write the processor**

Create `server/src/modules/newsletter/newsletter-draft.processor.ts`:

```ts
import { Processor, WorkerHost, InjectQueue } from '@nestjs/bullmq';
import { Logger, OnModuleInit } from '@nestjs/common';
import { Job, Queue } from 'bullmq';
import { NewsletterDraftService } from './newsletter-draft.service';
import { NEWSLETTER_DRAFT_QUEUE } from '../../common/queue/queue.constants';
import { registerRepeatable } from '../../common/queue/register-repeatable';

@Processor(NEWSLETTER_DRAFT_QUEUE)
export class NewsletterDraftProcessor extends WorkerHost implements OnModuleInit {
  private readonly logger = new Logger(NewsletterDraftProcessor.name);

  constructor(
    private readonly drafts: NewsletterDraftService,
    @InjectQueue(NEWSLETTER_DRAFT_QUEUE) private readonly queue: Queue,
  ) {
    super();
  }

  // Thursday 08:00 Europe/Sofia, registered once on worker boot (idempotent).
  async onModuleInit(): Promise<void> {
    await registerRepeatable(this.queue, 'weekly', '0 8 * * 4');
  }

  async process(job: Job): Promise<void> {
    if (job.name === 'weekly') {
      const ids = await this.drafts.eligibleTenantIds();
      for (const tenantId of ids) await this.queue.add('tenant', { tenantId });
      this.logger.log(`[newsletter-draft] fanned out ${ids.length} tenant job(s)`);
      return;
    }
    if (job.name === 'tenant') {
      const res = await this.drafts.generateForTenant((job.data as { tenantId: string }).tenantId);
      this.logger.log(`[newsletter-draft] ${JSON.stringify(res)}`);
      return;
    }
    this.logger.warn(`[newsletter-draft] unknown job name=${job.name}`);
  }
}
```

- [ ] **Step 3: Write the toggle DTO**

Create `server/src/modules/newsletter/dto/auto-settings.dto.ts`:

```ts
import { IsBoolean } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';

export class AutoSettingsDto {
  @ApiProperty()
  @IsBoolean()
  enabled: boolean;
}
```

- [ ] **Step 4: Wire the module**

In `server/src/modules/newsletter/newsletter.module.ts`, replace the whole file with:

```ts
import { Module } from '@nestjs/common';
import { BullModule } from '@nestjs/bullmq';
import { NewsletterService } from './newsletter.service';
import { NewsletterDraftService } from './newsletter-draft.service';
import { NewsletterCopyService } from './auto-draft.ai';
import { NewsletterDraftProcessor } from './newsletter-draft.processor';
import { NewsletterController } from './newsletter.controller';
import { AuthModule } from '../auth/auth.module';
import { BillingModule } from '../billing/billing.module';
import { NEWSLETTER_DRAFT_QUEUE } from '../../common/queue/queue.constants';
import { RUN_WORKERS } from '../../config/app-role';

@Module({
  imports: [
    AuthModule,
    BillingModule,
    BullModule.registerQueue({
      name: NEWSLETTER_DRAFT_QUEUE,
      defaultJobOptions: {
        attempts: 3,
        backoff: { type: 'exponential', delay: 5000 },
        removeOnComplete: true,
        removeOnFail: 100,
      },
    }),
  ],
  controllers: [NewsletterController],
  providers: [
    NewsletterService,
    NewsletterDraftService,
    NewsletterCopyService,
    ...(RUN_WORKERS ? [NewsletterDraftProcessor] : []),
  ],
})
export class NewsletterModule {}
```

- [ ] **Step 5: Add the controller endpoints**

In `server/src/modules/newsletter/newsletter.controller.ts`:

Add imports:

```ts
import { NewsletterDraftService } from './newsletter-draft.service';
import { AutoSettingsDto } from './dto/auto-settings.dto';
```

Inject the draft service into the constructor:

```ts
  constructor(
    private readonly newsletterService: NewsletterService,
    private readonly draftService: NewsletterDraftService,
  ) {}
```

Add these three endpoints inside the controller class (e.g. just above the `@Get('unsubscribe')` public route):

```ts
  /** Current auto-newsletter opt-in state for the tenant. */
  @Get('newsletter/auto-settings')
  @ApiBearerAuth()
  @UseGuards(JwtAuthGuard)
  getAutoSettings(@CurrentTenant() tenantId: string) {
    return this.draftService.getAutoNewsletter(tenantId);
  }

  /** Toggle the weekly auto-draft for the tenant. */
  @Patch('newsletter/auto-settings')
  @ApiBearerAuth()
  @UseGuards(JwtAuthGuard)
  setAutoSettings(@CurrentTenant() tenantId: string, @Body() dto: AutoSettingsDto) {
    return this.draftService.setAutoNewsletter(tenantId, dto.enabled);
  }

  /** Generate this tenant's auto-draft now (manual test of the weekly cron). */
  @Post('newsletter/auto-draft-test')
  @ApiBearerAuth()
  @UseGuards(JwtAuthGuard)
  autoDraftTest(@CurrentTenant() tenantId: string) {
    return this.draftService.generateForTenant(tenantId);
  }
```

(`Get`, `Post`, `Patch`, `Body`, `UseGuards`, `CurrentTenant`, `JwtAuthGuard`, `ApiBearerAuth` are already imported in this file.)

- [ ] **Step 6: Build to verify it compiles**

Run: `pnpm --filter @fermeribg/api build`
Expected: build succeeds, no TS errors.

- [ ] **Step 7: Run the full server suite**

Run: `pnpm --filter @fermeribg/api test`
Expected: all green (existing suite + Tasks 2–4 specs).

- [ ] **Step 8: Commit**

```bash
git add server/src/common/queue/queue.constants.ts server/src/modules/newsletter/newsletter-draft.processor.ts server/src/modules/newsletter/dto/auto-settings.dto.ts server/src/modules/newsletter/newsletter.module.ts server/src/modules/newsletter/newsletter.controller.ts
git commit -m "feat(newsletter): weekly auto-draft cron + toggle + test endpoints"
```

---

## Self-Review Notes

**Spec coverage:**
- Weekly Thu 08:00 Sofia cron → Task 5 processor (`'0 8 * * 4'`). ✓
- Per-tenant draft from fresh content → Task 4 `gatherFreshProducts` (new+available+bestseller fallback, cap 6) + `generateForTenant`. ✓
- AI copy + deterministic fallback → Task 3. ✓
- Block assembly (heading/intro/image+text/divider/button) → Task 2. ✓
- `createCampaign` draft + `auto_generated` dedup → Task 1 (column) + Task 4 (`hasPendingAutoDraft`, `markAutoGenerated`). ✓
- Notify farmer → Task 4 `notifyFarmer`. ✓
- Opt-in toggle backend (GET/PATCH) + eligibility → Task 4 (`get/setAutoNewsletter`, `eligibleTenantIds`) + Task 5 endpoints. ✓
- Manual test endpoint → Task 5 `POST newsletter/auto-draft-test`. ✓
- Never auto-sends (only draft) → no send call anywhere in the feature. ✓
- Skip on no-content / existing draft → Task 4 early returns. ✓
- Frontend checkbox = follow-up (descoped) → not in plan, per updated spec. ✓

**Placeholder scan:** none — every step has full code/commands. The one implementer NOTE (verify drizzle export names) is a safety check, not a missing definition; the names used are the real schema exports.

**Type consistency:** `DraftProduct`/`DraftCopy` defined in Task 2, consumed unchanged in Tasks 3–4. `GenResult` defined in Task 4, returned by the Task 5 test endpoint + processor. `assembleNewsletterBlocks(copy, products, shopUrl?)` and `writeCopy(farmName, products)` signatures consistent across tasks. `NEWSLETTER_DRAFT_QUEUE` defined Task 5 Step 1 before use. `createCampaign(tenantId,{subject,blocks})` matches the existing `NewsletterService` method.

**Test note (minor, deliberate):** `eligibleTenantIds` and the raw drizzle queries in `gatherFreshProducts` are covered by build + the manual `auto-draft-test` endpoint rather than unit tests (mocking the drizzle query-builder chain is brittle and low-value); the orchestration logic in `generateForTenant` — where the real branching lives — is fully unit-tested via the overridable db-method seams.

**Env note:** reuses `OPENAI_API_KEY` (AI copy, optional → fallback) and `PUBLIC_APP_URL` (notify link) — both already in the environment. No new required vars. `RUN_WORKERS` gates the processor (the cron only fires on the worker role).
