# Platform admin panel (port 3002). Copy to .env.
# Base URL of the ФермериБГ API. Server-only — the /bff proxy and /api/session
# routes read it; it is never exposed to the browser. Defaults to :3001.
API_URL=http://localhost:3001
